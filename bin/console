#!/usr/bin/env ruby
require 'bundler/setup'
require 'ginsu'
require 'pry'

#
# Console invocation via `pry` (gem install pry); This is NOT
# to be installed by `gem install` for the user; only for dev
# purposes.
#
Pry.start
